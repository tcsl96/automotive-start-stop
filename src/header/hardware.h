#include "../code/hardware.c"

bool checkHardware(
    bool speed_sensor,
    bool brake_sensor,
    bool temperature_sensor,
    bool battery_sensor,
    bool hood_sensor,
    bool trunk_sensor, 
    bool door_sensor, 
    bool seatbelt_sensor
);
